核心实现思路、关键技术栈以及代码落地的“抄作业”指南详细拆解如下。

模块一：全局状态管理 (The Bridge)

核心思路：

这是一个多线程系统。视觉模块在不断更新状态，对话模块在需要时读取状态。我们需要一个线程共享的内存区。



实现方式：

使用一个 Python 字典 GLOBAL_STATE 作为共享容器。

视觉线程只负责写（Write）：每隔几帧更新里面的情绪标签。

对话线程只负责读（Read）：录音结束时，从里面取出当前最新的情绪。

代码骨架：

Python



# shared_state.py

GLOBAL_STATE = {

"visual_emotion": "Neutral", # 当前捕捉到的表情

"visual_confidence": 0.0, # 置信度 (0.0 - 1.0)

"frame_counter": 0, # 帧计数器（用于跳帧优化）

"last_update_time": 0 # 最后更新时间

}

模块二：视觉感知模块 (The Eyes)

核心思路：

利用 Gradio 的 streaming 特性，对视频流进行抽帧分析。不要分析每一帧，否则会占用大量 GPU 资源导致对话生成变慢。



技术栈：OpenCV, Transformers Pipeline, dima806/facial_emotions_image_detection (ViT)

实现步骤：

加载模型：程序启动时加载 Pipeline 到 GPU (device=0)。

跳帧策略：使用模运算 frame_count % 5 == 0，即每 5 帧（约 0.16秒）推理一次。

格式转换：Gradio 传入的是 RGB（Numpy array），模型通常可以直接吃，或者转 PIL。

可视化反馈：这是加分项！利用 cv2.putText 把检测结果直接画在视频帧上，让用户看到系统在工作。

避坑指南：

OpenCV 颜色坑：Gradio 图片是 RGB，OpenCV 画图用 BGR。处理前先 cvtColor 转 BGR，画完再转回 RGB，否则人脸是蓝色的。

模块三：听觉感知模块 (The Ears)

核心思路：

分为两个并行任务：听内容 (ASR) 和 听语气 (SER)。这两个任务都针对同一个 input.wav 文件进行。



子模块 A：ASR (语音转文字)

技术栈：openai-whisper (Small 模型)

实现逻辑：

直接调用 model.transcribe(audio_path)。

获取结果中的 result['text']。

优化：如果你觉得 Whisper Small 慢，可以用 base 模型，或者使用 faster-whisper 库。

子模块 B：SER (语音情感识别)

技术栈：transformers (Wav2Vec2), librosa

模型推荐：superb/wav2vec2-base-superb-er 或 ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition

实现逻辑：

重采样：模型通常要求 16000Hz 采样率。使用 librosa.load(path, sr=16000) 读取。

推理：输入模型获取 Logits（原始分数）。

归一化：使用 torch.softmax 将分数转为概率（0-100%）。

取最大：选概率最高的那个标签（如 Sadness）。

模块四：认知与融合模块 (The Brain)

核心思路：

这是你的算法创新点。不要做复杂的神经网络融合，采用**“基于置信度的规则融合” (Rule-based Confidence Fusion)**。



融合策略逻辑：

获取输入：视觉情绪 (V_emo, V_score)，音频情绪 (A_emo, A_score)。

冲突消解：

规则 1 (视觉优先)：如果 V_score > 0.85，认为表情非常明显，直接采纳 V_emo。

规则 2 (音频补位)：如果 V_score 较低（面瘫/表情平淡），但 A_score > 0.8（语气强烈，如哭腔），采纳 A_emo。

规则 3 (文本兜底)：如果视听都不明显，分析文本关键词（如“想死”），强制判定为负面。

输出：Final_Emotion。

LLM 交互逻辑：

Prompt 构建：不要只给 LLM 文本，要显式地告诉它情绪。

模板：

Plaintext



[系统指令] 你是心理咨询师。

[感知数据] 用户正处于【{Final_Emotion}】情绪中。

[用户原话] "{User_Text}"

[回复要求] 请先共情，再简短回复。

模块五：表达模块 (The Mouth)

核心思路：

生成的文字必须变成语音，才能形成“面对面”的感觉。使用异步 IO 避免阻塞主线程。



技术栈：edge-tts (微软免费接口，Python库)

实现逻辑：

接收 LLM 的 reply_text。

生成唯一文件名（避免浏览器缓存旧音频），例如 output/reply_{timestamp}.mp3。

调用 edge-tts 保存文件。

返回文件路径给 Gradio 的 Audio 组件进行自动播放。

避坑指南：

TTS 生成需要 1-2 秒，此时界面要显示 loading 状态。

模块六：交互界面模块 (The Face)

核心思路：

使用 Gradio 将上述模块串联。利用 Gradio 的事件监听机制。



布局设计：

gr.Image(streaming=True)：作为“眼睛”，每秒刷新 30 次。

gr.Audio(source="microphone")：作为“耳朵”，录音结束触发逻辑。

gr.Textbox：作为“系统日志”，实时打印融合过程（证明工作量）。

事件绑定：

视频流绑定：video_input.change(fn=process_video_stream, ...)

对话流绑定：audio_input.stop_recording(fn=process_dialogue, ...)

总结：各类数据的流转路径

视频数据：摄像头 -> Gradio -> process_video 函数 -> ViT 模型 -> 画框 & 更新全局变量。

音频数据：麦克风 -> Gradio -> process_dialogue 函数 -> Whisper & Wav2Vec -> 情绪 & 文本。

融合数据：全局变量(视觉) + 音频情绪 -> 融合算法 -> LLM Prompt -> Qwen2.5 -> 回复文本。

反馈数据：回复文本 -> Edge-TTS -> MP3 -> 浏览器播放。