一、课题名称

多模态情感感知共情对话系统的设计与实现

二、项目总体目标

本项目面向自然人机交互场景，设计并实现一个集视觉表情识别、语音识别、语音情感识别、大语言模型共情回复、语音合成、用户管理、会话持久化和运行监控于一体的多模态情感对话系统。系统通过摄像头实时捕获用户面部表情，通过麦克风采集用户语音内容与声学情绪，并将视觉、听觉和文本语义信息共同提供给本地大语言模型，生成更贴近用户情绪状态的中文共情回复，最后通过语音合成完成闭环交互。

系统当前以 Python 为主要开发语言，使用 Gradio 构建前端交互界面，使用 SQLite 保存用户、会话、消息、多模态特征和系统监控数据，主要面向本地 GPU 环境部署运行。

三、系统主要功能模块

1. 视觉情感感知模块

视觉情感感知模块位于 modules/vision.py，负责从 Gradio 摄像头输入中接收实时图像帧，并调用基于 Transformers 的图像分类模型进行面部情绪识别。模块使用 AutoImageProcessor 和 AutoModelForImageClassification 加载本地视觉情绪模型，支持 CUDA 自动检测，并优先使用 FP16 推理。

该模块的主要功能包括：

（1）实时接收摄像头图像帧；
（2）将图像转换为张量并在 GPU 上完成预处理；
（3）通过视觉情绪模型输出情绪类别、置信度和 Top-K 概率分布；
（4）将视觉识别结果写入全局状态；
（5）记录单帧视觉推理耗时，包括数据搬运、预处理、模型推理、后处理和总耗时；
（6）在用户录音期间缓存连续视觉识别结果，为语音结束后的多模态判断提供参考。

当前实现中，录音期间的视觉情绪并非只取某一帧结果，而是将录音过程中的多帧视觉识别结果加入缓存，并在用户停止录音后通过众数方式得到本轮对话的视觉情绪基调。

2. 听觉感知模块

听觉感知模块位于 modules/audio.py，包含语音识别和语音情感识别两个子功能。

语音识别部分使用 faster-whisper 加载本地 Whisper 模型，将用户录音转写为中文文本。模型配置默认使用 CUDA 和 float16，并在转写时指定中文语言环境，以提高中文语音输入的识别稳定性。

语音情感识别部分使用 FunASR 的 emotion2vec 模型分析用户语音中的声学情绪。模块会读取 emotion2vec 输出的标签和分数，将其映射为 Angry、Fear、Happy、Neutral、Sad、Surprise 六类目标情绪，并对目标情绪概率进行归一化，返回主导情绪、置信度和概率分布。

该模块为后续共情回复提供两类输入：

（1）用户说了什么：由 Whisper 转写得到的文本；
（2）用户听起来是什么情绪：由 emotion2vec 提取的声学情感结果。

3. 大语言模型共情生成模块

大语言模型模块位于 modules/llm.py，负责结合用户文本、视觉情绪、音频情绪和系统初步判断结果生成回复。系统通过 Transformers 加载本地 Qwen 模型，使用 device_map="auto" 和 bfloat16 方式部署，以适配本地 GPU 推理环境。

本模块的核心流程为：

（1）构建系统提示词，指定模型以专业心理咨询和共情陪伴的语气生成中文回复；
（2）将历史对话、用户语音转写文本、视觉情绪、音频情绪和初步情绪判断拼接为当前输入；
（3）要求模型输出 JSON 格式结果，包含 emotion 和 response 两个字段；
（4）解析模型输出，提取模型最终判断的情绪标签和中文共情回复；
（5）当 JSON 解析异常时，提供兜底回复，保证前端交互不中断。

该模块是系统的语义决策中心，视觉与听觉模块提供的多模态信息会作为上下文参考参与回复生成，使系统不只依赖语音文字内容，也能参考用户的面部表情和语音情绪。

4. 语音合成反馈模块

语音合成模块位于 modules/tts.py，负责将大语言模型生成的中文文本转换为可播放的语音文件。当前系统使用 edge-tts 进行语音合成，生成的 mp3 文件保存到 assets/output_tts 目录，并在 Gradio 前端中自动播放。

系统支持用户个性化选择音色，目前在 app.py 中预设了标准普通话、东北普通话、粤语、陕西话四类音色。用户可以在个人设置页面保存偏好音色，也可以试听当前选择的音色。对话回复时，系统会优先使用用户保存的偏好音色进行合成。

5. 前端交互与业务流程模块

前端主程序位于 app.py，使用 Gradio Blocks 构建完整的人机交互界面。当前系统不只是单一聊天页面，还包含用户登录注册、普通用户对话界面、账户设置页面和管理员控制台。

普通用户侧主要功能包括：

（1）用户登录与注册；
（2）摄像头实时视觉情绪识别；
（3）麦克风语音输入；
（4）语音识别文本显示；
（5）多模态情绪分析结果展示；
（6）聊天历史显示；
（7）系统回复语音自动播放；
（8）新建对话、清空对话、加载历史会话；
（9）导出聊天记录；
（10）修改昵称、个人资料和偏好音色；
（11）修改密码与退出登录。

管理员侧主要功能包括：

（1）管理员登录与邀请码注册；
（2）查看系统用户列表；
（3）删除普通用户，并级联删除其历史会话和相关数据；
（4）为指定用户重置密码；
（5）查看指定用户的使用概览；
（6）查看指定用户的系统监控日志；
（7）查看服务器 GPU 显存占用情况。

6. 数据库与会话持久化模块

数据库管理模块位于 utils/db_manager.py，使用 SQLite3 保存系统运行所需的数据。数据库文件路径为 assets/system_data.db。

当前数据库主要包含以下数据表：

（1）users：保存用户账号、密码哈希、角色和注册时间；
（2）user_profiles：保存用户昵称、个人资料和偏好语音音色；
（3）sessions：保存用户会话；
（4）messages：保存每轮对话中的用户消息和系统回复；
（5）multimodal_features：保存每轮回复关联的视觉情绪、视觉置信度、音频情绪、音频置信度和大模型最终判断；
（6）system_metrics：保存系统模块运行日志、GPU 显存占用、关联用户和关联会话。

该模块还提供注册、登录、密码修改、管理员重置密码、删除用户、创建会话、读取历史消息、导出会话记录、写入监控日志等接口。

7. 系统资源监控模块

系统资源监控模块位于 utils/monitor.py，主要使用 torch 和 nvidia-ml-py 调用 NVML 接口读取 GPU 显存占用。前端会在用户对话区和管理员监控区显示当前 GPU 显存状态。

该模块的作用包括：

（1）监测 CUDA 是否可用；
（2）初始化 NVIDIA NVML 句柄；
（3）读取 GPU 已用显存和总显存；
（4）为用户侧和管理员侧界面提供资源状态；
（5）辅助记录系统运行日志。

8. 性能测试与结果记录模块

性能日志模块位于 utils/performance_logger.py，负责将视觉推理和完整对话流程的耗时数据写入 CSV 文件。结果默认保存到 assets/performance_results 目录。

当前系统记录三类性能文件：

（1）vision_performance.csv：记录单帧视觉处理性能，包括数据搬运、预处理、推理、后处理和总耗时；
（2）dialogue_performance.csv：记录单轮完整对话流程耗时，包括 ASR、音频情绪识别、视觉决策、回复生成、TTS、数据库写入和总耗时；
（3）performance_summary.csv：自动汇总视觉和对话流程的平均耗时。

该模块为毕业设计中的系统性能分析、对比实验和运行评估提供了数据基础。

四、系统运行流程

系统启动时，app.py 会依次尝试加载视觉模型、Whisper 模型、emotion2vec 模型和 Qwen 大语言模型，并初始化 SQLite 数据库结构。随后 Gradio 服务在 7863 端口启动。

普通用户完成登录后，可进入对话界面。摄像头组件会持续触发视觉识别流程，系统实时显示当前视觉情绪、置信度、概率条和已处理帧数。当用户按下录音并开始说话时，系统同步缓存录音期间的视觉情绪结果。用户停止录音后，系统依次完成语音转写、音频情绪识别、录音期间视觉情绪综合、大语言模型回复生成、语音合成、数据库保存和性能日志写入，最后将文本回复和语音回复同时返回给用户。

管理员登录后，可进入独立管理后台，对用户账号、密码、用户活动记录和系统日志进行管理和查看。

五、项目当前特点

（1）采用视觉、语音、文本三类信息共同参与情绪理解；
（2）支持摄像头实时视觉情绪识别和录音阶段视觉情绪缓存；
（3）支持 Whisper 中文语音识别和 emotion2vec 声学情感识别；
（4）使用本地 Qwen 模型生成中文共情回复；
（5）使用 Edge-TTS 输出自然语音，形成语音交互闭环；
（6）具有完整的用户注册、登录、个人设置、历史会话和导出功能；
（7）具有管理员后台，可进行用户管理、密码重置和运行日志查看；
（8）使用 SQLite 保存账号、会话、消息、多模态特征和监控日志；
（9）记录视觉模块和完整对话流程的性能数据，便于论文实验分析；
（10）面向本地 GPU 环境优化，视觉模块使用 FP16 推理，LLM 使用 bfloat16 与自动设备映射。
