1. 视觉动态感知模块 (Vision Perception)
本模块负责“看懂”用户的面部表情及其动态变化。系统直接调用本地计算机摄像头实时采集视频流，利用 Vision Transformer (ViT) 模型对视频帧进行人脸检测与特征提取。

核心升级：不再仅仅输出一个死板的最高分情绪标签，而是持续记录用户在整个说话期间（如 5 秒内）的面部情绪概率分布时间序列。在用户结束录音时，系统会对这期间积攒的所有有效帧进行概率平均，输出一个精确到百分比的综合面部情绪画像（例如：“50% 中性，30% 悲伤，20% 快乐”）。

2. 听觉深度感知模块 (Audio Perception)
本模块负责“听懂”用户的语言内容与语气情感，细分为两个并行运作的子功能：

2.1 语义转录 (ASR)：利用本地部署的 Faster-Whisper (Large-v3) 模型，将麦克风采集到的用户语音高精度转换为文本，作为对话的直接内容输入。

2.2 声学情感抽取 (SER)：利用音频情感模型（Emotion2Vec），独立于文本内容，直接分析用户发音时的声学特征（音高、语速、颤音等）。核心升级：同样输出各情感维度的概率分布（例如：“80% 悲伤，20% 激动”）。

3. 多模态语义融合与共情生成模块 (LLM Semantic Fusion & Generation)
这是整个系统重构后的核心“大脑”，将原有的“规则融合”与“对话生成”合二为一。
本模块基于本地部署的 Qwen3.5-9B 大语言模型。系统会在后台自动构建一个高度结构化的动态 Prompt（提示词），将前置感知模块收集到的所有“碎片信息”打包喂给大模型。

输入构造：Prompt 中不仅包含用户的文本（说什么），还会清晰地列出视觉概率分布（看起来怎样）和听觉概率分布（听起来怎样）。

语义级推理：当遇到模态冲突（例如：视觉检测到 60% 的笑容，但听觉检测到 80% 的悲伤）时，强制要求大模型在生成回复前，先进行“内部思考”（Chain of Thought），推断用户是否在伪装坚强或带有反讽意味。

输出：最终生成高度贴切、具备深度共情和安抚性质的文本回复。

4. 语音合成闭环模块 (TTS Feedback)
为了实现“面对面”的自然交流感，本模块负责将 Qwen 生成的共情文本转换为语音。采用 Edge-TTS 技术，生成语调自然、流畅的语音流并自动播放，使用户无需时刻紧盯屏幕阅读文字，完成纯语音+视觉交互的自然闭环。

5. 用户交互界面模块 (Front-end UI)
面向最终用户的纯前端可视化窗口，基于 Gradio 构建。该界面专注于用户体验，严格不包含任何后台管理或管理员界面的冗余设计。

感知反馈区：实时显示摄像头的监控画面，以及系统当前捕捉到的多模态概率雷达图。

对话沉浸区：提供一键式的语音录制交互按钮、清爽的对话历史气泡记录，以及系统回复语音的自动播放功能。

6. 硬件资源与状态监控模块 (Resource Monitor)
此模块在后台静默运行，是保障系统稳定性的关键。由于整个系统（ViT + Whisper + Qwen3.5-9B）都需要在本地单张显卡上并发运行，该模块会通过 pynvml 实时监控显存分配。