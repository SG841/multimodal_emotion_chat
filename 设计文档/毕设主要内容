1.视觉感知模块本模块：
负责“看懂”用户的面部表情。系统通过摄像头实时采集用户的视频流，利用 Vision Transformer 架构的深度学习模型，对视频帧进行人脸检测与特征提取。该模块采用跳帧检测策略（每5帧处理一次）以降低算力负载，实时输出用户的面部情绪标签（如开心、悲伤、愤怒、中性等）及其置信度分数，并将其写入系统的全局共享状态中。
2.听觉感知模块：
本模块负责“听懂”用户的语言内容与语气情感，细分为两个子功能：
2.1语音识别 (ASR)：利用 Faster-Whisper 模型将用户的语音信号转换为文本信息，作为后续对话生成的语义输入。
2.2语音情感识别 (SER)：利用 Wav2Vec2.0 模型分析语音的声学特征（如音高、语速、停顿），判断用户说话时的语气状态（如激动、低沉、哭腔），弥补单一文本分析在情感捕捉上的缺失。
3.多模态融合决策模块：
系统的核心“大脑”，负责解决视觉与听觉的冲突。该模块通过基于置信度的规则融合算法 ，综合分析视觉情绪与听觉情绪的概率分布。当视觉置信度较高时（如面部表情明显），优先采纳视觉结果；当视觉特征模糊（如面无表情）但语音情感强烈时，动态提升听觉权重；最终输出一个唯一的“全局情绪状态”，作为对话系统的上下文约束。
4.共情对话生成模块：
本模块基于部署在本地的 Qwen2.5 (7B) 大语言模型。模块接收融合后的情绪状态标签和用户的文本内容，通过精心设计的提示词工程，引导模型扮演“心理咨询师”的角色。与传统问答不同，该模块生成的回复不仅关注逻辑回答，更侧重于对用户情绪的共情、安抚与引导。
5.语音合成反馈模块：
为了实现“面对面”的自然交流感，本模块负责将大模型生成的文本回复转换为语音。采用 Edge-TTS 技术，生成语调自然、流畅的语音流，并自动在前端播放，使用户无需阅读文字即可获得反馈，完成语音交互的闭环。
6.交互界面模块：
模块基于 Gradio 框架构建，面向最终用户。界面分为左侧“感知区”和右侧“交互区”：
感知区：实时显示摄像头画面及系统识别到的当前情绪标签（可视化反馈）。
交互区：提供语音录入按钮、对话历史记录显示以及系统回复的自动播放功能。
7.系统状态监控模块：
此模块主要服务于开发者与演示场景。它在后台实时记录各模态的推理耗时、显存占用情况以及融合算法的决策日志。通过该模块，可以直观地展示系统是如何在视觉和听觉之间做权衡的。